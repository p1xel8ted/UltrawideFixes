//Roll blur
cbuffer cbFeedbackBlurParam : register(b0)
{
  float src_coeff : packoffset(c0);
  float dst_coeff : packoffset(c0.y);
}

SamplerState currTex_s_s : register(s0);
SamplerState prevTex_s_s : register(s1);
Texture2D<float4> currTex : register(t0);
Texture2D<float4> prevTex : register(t1);

void main(float4 v0 : SV_POSITION0, linear noperspective float2 v1 : TEXCOORD0, out float4 o0 : SV_TARGET0, out float4 o1 : SV_TARGET1)
{
  //Just stub this shit out.
  o0.xyzw = currTex.Sample(currTex_s_s, v1.xy).xyzw;
  o1.xyzw = currTex.Sample(currTex_s_s, v1.xy).xyzw;
  return;
}